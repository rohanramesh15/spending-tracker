# SPA fallback for Cloudflare Pages: serve index.html for client-side routes so a direct
# hit or refresh on a deep link (e.g. the Plaid OAuth return to /settings?oauth_state_id=…)
# loads the app instead of a 404. Real assets (/assets/*) are served before this fallback.
/*    /index.html   200
