/**
 * Settings — connected accounts, categories, CSV import, export
 * (user-flow.md §9). Scaffold stub.
 */
export default function SettingsPage() {
  return (
    <section>
      <h1 className="text-xl font-semibold">Settings</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Extraction provider: Gemini — free tier.
      </p>
    </section>
  );
}
