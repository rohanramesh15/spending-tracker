import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function ScanScreen() {
  return <ScreenPlaceholder title="Scan a receipt" step="step 8 of the Expo conversion" />;
}
